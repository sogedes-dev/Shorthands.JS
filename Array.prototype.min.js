/**
 * Returns the lowest value in the array
 * @method
 * @instance
 * @memberOf Array
 * @name min
 * @returns {number}
 */

Object.defineProperty(Array.prototype,"min",{value:function min() {
    return Math.min(...this);
}});
